### Update README with quick launch section

Add the following section near the top or after installation instructions:

```md
## ▶️ Launch shortcuts

### macOS / Linux
```bash
chmod +x start.sh
./start.sh
```

### Windows
```bat
start.bat
```

This script creates a local virtual environment, installs dependencies, and starts the Flask app.
```
